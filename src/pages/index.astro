---
import Layout from '../layouts/Layout.astro';
import Section from '../components/Section.astro';
import Hero from '../components/Hero.astro';
import StatBand from '../components/StatBand.astro';
import ServiceCard from '../components/ServiceCard.astro';
import Testimonial from '../components/Testimonial.astro';
import CTA from '../components/CTA.astro';
import BeforeAfterGallery from '../components/BeforeAfterGallery.astro';
import { services, sectors, site } from '../data/site';
import { images } from '../data/images';
import { pickWork } from '../data/work';

const homeWork = pickWork([
  'redSafetyFloor',
  'marbleHallway',
  'oakRestoration',
  'churchFlagstone',
  'killarneyPool',
  'commercialLobby',
  'blueSchoolFloor',
  'pubFloor',
]);

const homeJsonLd = [
  JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    name: site.name,
    url: site.url,
    inLanguage: 'en-IE',
    publisher: { '@type': 'Organization', name: site.name },
  }),
  JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'ItemList',
    name: 'Services offered by SaniSafe Solutions',
    itemListElement: services.map((s, idx) => ({
      '@type': 'ListItem',
      position: idx + 1,
      url: new URL(`/services/${s.slug}`, site.url).toString(),
      name: s.title,
    })),
  }),
];

const otherServices = services.filter((s) => s.slug !== 'sliptest');
const processSteps = [
  { n: '01', title: 'Survey & agreed treatment', body: 'On-site visit. Pendulum readings, photographs, condition assessment.' },
  { n: '02', title: 'Quote', body: 'Fixed-price quote, with scope, timing and access notes.' },
  { n: '03', title: 'Restoration / treatment', body: 'Agreed programme of works carried out with minimal disruption.' },
  { n: '04', title: 'Aftercare', body: 'Care, training and guidance provided, and a follow-up pendulum test where slip resistance was an issue.' },
];

const testimonials = [
  {
    quote:
      'SaniSafe removed limescale build-up on our pool deck that no other company before them could. Their aftercare has kept the facility like new ever since.',
    name: 'Facilities Manager',
    role: 'Fitzgerald Woodlands House Hotel, Adare, Co. Limerick',
  },
  {
    quote:
      'Oak bar floor restored overnight. Guests commented that the floor looked brand new.',
    name: 'Operations Manager',
    role: 'Westport Woods Hotel, Co. Mayo',
  },
  {
    quote:
      'The SaniSafe lads transformed our pool deck back to its former glory and ensured our customers are safe with their anti-slip treatment.',
    name: 'Leisure Centre Manager',
    role: 'The Nuremore Hotel, Co. Monaghan',
  },
  {
    quote:
      'My floors are like new! We were about to replace our marble floor because we simply couldn\'t get it as clean as it was. SaniSafe saved us a lot of money and transformed the look of my home.',
    name: 'Amy Kelly',
    role: 'Homeowner, Lahinch, Co. Clare',
  },
];
---

<Layout
  title="SaniSafe Solutions — Floor Restoration, Anti-Slip & Pendulum Slip Testing | Ireland"
  description="Marble, wood and travertine restoration plus anti-slip treatments, deep cleaning and pendulum slip testing for hospitals, commercial spaces and homes. SaniSafe Solutions — Ennis, working nationwide."
  jsonLd={homeJsonLd}
>
  {/* ---------- HERO ---------- */}
  <Hero
    headline="Floor Restoration & Slip Safety Specialists"
    subhead="Restore worn floors, reduce slip risks, and receive professional testing reports accepted by insurers and auditors. Serving commercial and domestic clients across Ireland."
    eyebrow="Commercial & domestic · Ennis, Co. Clare — working nationwide"
    variant="navy"
    imageKey="heroHome"
    eager
  >
    <a slot="ctas" href={site.phoneHref} class="btn-yellow text-lg" aria-label={`Call SaniSafe now on ${site.phoneDisplay}`}>
      <svg aria-hidden="true" viewBox="0 0 20 20" class="h-5 w-5"><path fill="currentColor" d="M2 4.5C2 3.7 2.7 3 3.5 3H6c.6 0 1.1.4 1.3 1l.9 3c.1.5 0 1-.4 1.3L6.6 9.6a11 11 0 0 0 3.8 3.8l1.3-1.2c.3-.4.8-.5 1.3-.4l3 .9c.6.2 1 .7 1 1.3v2.5c0 .8-.7 1.5-1.5 1.5A14.5 14.5 0 0 1 2 4.5z"/></svg>
      Call Now <span class="font-extrabold">{site.phoneDisplay}</span>
    </a>
    <a slot="ctas" href="/contact" class="btn-yellow-outline">Get a Free Floor Assessment</a>

    <p class="mt-8 text-sm text-white/70 max-w-md">
      Protection You Can Trust. Safety You Can Stand On!
    </p>
  </Hero>

  {/* ---------- TRUST BAND ---------- */}
  <div class="bg-surface-offwhite border-b border-surface-grey">
    <div class="container-page py-6 sm:py-7">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.18em] text-ink-slate">
        Trusted by hotels, leisure centres, healthcare, schools &amp; homes across Ireland
      </p>
      <div class="mt-4 flex flex-wrap items-center justify-center gap-x-8 gap-y-3 text-sm font-semibold text-brand-navy">
        <span>Fitzgerald Woodlands House Hotel</span>
        <span aria-hidden="true" class="text-surface-grey">·</span>
        <span>Westport Woods Hotel</span>
        <span aria-hidden="true" class="text-surface-grey">·</span>
        <span>The Nuremore Hotel</span>
        <span aria-hidden="true" class="text-surface-grey">·</span>
        <span>Killarney Leisure Centre</span>
      </div>
    </div>
  </div>

  {/* ---------- SERVICES GRID ---------- */}
  <Section variant="white" id="services">
    <div class="max-w-2xl">
      <p class="eyebrow">What we do</p>
      <h2 class="h-section mt-3">Restoration, deep cleaning and slip safety, under one roof.</h2>
      <p class="lead mt-4">
        Six core services that cover the lifecycle of your floor — from a deep restoration of marble, wood and
        travertine through to anti-slip treatments and deep cleaning. Our pendulum slip-testing service,
        SlipTest, is detailed below.
      </p>
    </div>

    <div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {otherServices.map((s) => (
        <ServiceCard
          title={s.title}
          promise={s.promise}
          href={`/services/${s.slug}`}
          imageKey={s.imageKey}
          accent={s.accent}
        />
      ))}
    </div>
  </Section>

  {/* ---------- RESTORE, DON'T REPLACE ---------- */}
  <section class="bg-brand-yellow text-brand-navy">
    <div class="container-page py-14 sm:py-16 flex flex-col gap-6 lg:flex-row lg:items-center lg:justify-between">
      <div class="max-w-2xl">
        <p class="text-xs font-bold uppercase tracking-[0.18em] text-brand-navy/80">Restore, don't replace</p>
        <h2 class="mt-3 font-display font-extrabold text-3xl sm:text-4xl tracking-tight">Save thousands by restoring the floor you already have.</h2>
        <p class="mt-3 text-lg text-brand-navy/90 max-w-xl">
          Most worn marble, stone, hardwood and tiled floors don't need ripping out. Restoring the existing floor
          saves the cost and disruption of a full replacement — often in a fraction of the time, and brought back to
          life in place.
        </p>
      </div>
      <a href="/contact" class="btn-navy shrink-0">Get a Free Floor Assessment</a>
    </div>
  </section>

  {/* ---------- SECTORS BAND ---------- */}
  <Section variant="offwhite" id="sectors">
    <div class="max-w-2xl">
      <p class="eyebrow">Sectors we serve</p>
      <h2 class="h-section mt-3">Different floors. Same standards.</h2>
      <p class="lead mt-4">
        We work to the same high standard across hospitals, commercial sites and homes — the priorities may change
        with the brief, but the results stay the same.
      </p>
    </div>

    <div class="mt-10 grid gap-6 md:grid-cols-3">
      {sectors.map((s) => {
        const img = images[s.imageKey];
        return (
          <a href={`/sectors#${s.slug}`} class="group block card hover:shadow-lg transition-shadow">
            <div class="relative aspect-[3/2] overflow-hidden">
              <img
                src={img.src}
                alt={img.alt}
                width={img.width}
                height={img.height}
                loading="lazy"
                class="absolute inset-0 h-full w-full object-cover transition-transform duration-500 group-hover:scale-[1.03]"
              />
            </div>
            <div class="p-6">
              <h3 class="font-display font-extrabold text-xl text-brand-navy">{s.title}</h3>
              <p class="mt-2 text-ink-slate">{s.pitch}</p>
              <span class="mt-4 inline-flex items-center gap-1 text-sm font-bold text-brand-navy">
                See sector detail
                <svg aria-hidden="true" viewBox="0 0 20 20" class="h-4 w-4"><path fill="currentColor" d="M7.5 5 14 11.5 7.5 18l-1.5-1.5 5-5-5-5z"/></svg>
              </span>
            </div>
          </a>
        );
      })}
    </div>
  </Section>

  {/* ---------- RECENT WORK (BEFORE / AFTER) ---------- */}
  <Section variant="white" id="work">
    <div class="max-w-2xl">
      <p class="eyebrow">Recent work</p>
      <h2 class="h-section mt-3">Before and after, on recent jobs.</h2>
      <p class="lead mt-4">
        Whether you simply need a deep clean or a full restoration, we have a solution for every situation. We have
        restored and brought floors of all kinds back to life at a fraction of the price of replacing them — across
        hospitals, schools, leisure centres, bars and homes.
      </p>
    </div>

    <div class="mt-10">
      <BeforeAfterGallery items={homeWork} />
    </div>

    {/* Featured case study */}
    <div class="mt-12 grid items-center gap-8 rounded-2xl border border-surface-grey bg-surface-offwhite p-6 sm:p-8 lg:grid-cols-12">
      <div class="lg:col-span-7">
        <p class="eyebrow">Case study · Leisure</p>
        <h3 class="mt-2 font-display font-extrabold text-2xl text-brand-navy">Pool-deck limescale removed where others couldn't.</h3>
        <dl class="mt-6 grid gap-5 sm:grid-cols-3">
          <div>
            <dt class="text-xs font-bold uppercase tracking-[0.18em] text-ink-slate">Problem</dt>
            <dd class="mt-1 text-ink-charcoal">Heavy limescale build-up on the pool deck that previous contractors couldn't shift.</dd>
          </div>
          <div>
            <dt class="text-xs font-bold uppercase tracking-[0.18em] text-ink-slate">Solution</dt>
            <dd class="mt-1 text-ink-charcoal">Specialist deep clean and restoration, followed by an ongoing aftercare regime.</dd>
          </div>
          <div>
            <dt class="text-xs font-bold uppercase tracking-[0.18em] text-ink-slate">Result</dt>
            <dd class="mt-1 text-ink-charcoal">The deck cleaned up and the facility has stayed "like new" ever since.</dd>
          </div>
        </dl>
        <p class="mt-6 text-sm font-semibold text-ink-slate">— Facilities Manager, Fitzgerald Woodlands House Hotel, Adare, Co. Limerick</p>
      </div>
      <div class="lg:col-span-5">
        <img
          src="/images/work/killarney-pool-deck.webp"
          alt="Leisure-centre pool deck — the kind of wet tiled surface where we remove limescale and restore slip safety"
          width="800"
          height="533"
          loading="lazy"
          decoding="async"
          class="aspect-[3/2] w-full rounded-xl object-cover"
        />
      </div>
    </div>

    <div class="mt-10">
      <a href="/contact" class="btn-primary">Get a Free Floor Assessment</a>
    </div>
  </Section>

  {/* ---------- PROCESS ---------- */}
  <Section variant="white" id="process">
    <div class="max-w-2xl">
      <p class="eyebrow">Our process</p>
      <h2 class="h-section mt-3">Four steps. No surprises.</h2>
    </div>

    <ol class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
      {processSteps.map((step) => (
        <li class="card p-6 h-full flex flex-col">
          <span class="font-display font-extrabold text-4xl text-brand-blue">{step.n}</span>
          <h3 class="mt-3 font-display font-extrabold text-lg text-brand-navy">{step.title}</h3>
          <p class="mt-2 text-ink-slate">{step.body}</p>
        </li>
      ))}
    </ol>
  </Section>

  {/* ---------- TESTIMONIALS ---------- */}
  <Section variant="offwhite" id="testimonials">
    <div class="max-w-2xl">
      <p class="eyebrow">What our clients say</p>
      <h2 class="h-section mt-3">Excellent work and fantastic aftercare.</h2>
    </div>

    <div class="mt-10 grid gap-6 md:grid-cols-2">
      {testimonials.map((t) => (
        <Testimonial quote={t.quote} name={t.name} role={t.role} />
      ))}
    </div>
  </Section>

  {/* ---------- WHY SLIP SAFETY MATTERS ---------- */}
  <Section variant="white" id="slip-safety">
    <div class="max-w-2xl">
      <p class="eyebrow">Why slip safety matters</p>
      <h2 class="h-section mt-3">A safe floor protects people — and your business.</h2>
    </div>
    <div class="mt-10 grid gap-6 md:grid-cols-3">
      <div class="card p-6">
        <h3 class="font-display font-extrabold text-lg text-brand-navy">A common, costly risk</h3>
        <p class="mt-2 text-ink-slate">Slips, trips and falls are among the most frequent causes of workplace injury and personal-injury claims in Ireland and the UK.</p>
      </div>
      <div class="card p-6">
        <h3 class="font-display font-extrabold text-lg text-brand-navy">Your duty as an occupier</h3>
        <p class="mt-2 text-ink-slate">Health-and-safety law expects reasonable steps to keep floors safe. Acting before an accident is far cheaper than defending one after.</p>
      </div>
      <div class="card p-6">
        <h3 class="font-display font-extrabold text-lg text-brand-navy">Proof you can document</h3>
        <p class="mt-2 text-ink-slate">Independent pendulum (PTV) testing gives you a defensible, written record of floor safety for insurers, auditors and, if it ever comes to it, a court.</p>
      </div>
    </div>
  </Section>

  {/* ---------- STAT BAND ---------- */}
  <StatBand
    number="95%"
    caption="of major slips and falls result in broken bones"
    attribution="HSE (UK) estimate, 2025"
    body="SaniSafe Solutions carries out pendulum slip-resistance testing under our SlipTest service so your floors don't pose a safety risk."
    ctaHref="/services/sliptest"
    ctaLabel="Learn about SlipTest"
  />

  {/* ---------- CTA ---------- */}
  <CTA />

  {/* ---------- SLIPTEST EXPLAINER ---------- */}
  <Section variant="white" id="sliptest-explainer">
    <div class="grid gap-10 lg:grid-cols-12 items-center">
      <div class="lg:col-span-7">
        <div class="flex items-center gap-4">
          <img
            src={site.subBrand.logo}
            alt="SlipTest — Confidence in every step"
            width="80"
            height="80"
            loading="lazy"
            class="h-16 w-16 sm:h-20 sm:w-20 object-contain bg-white rounded-md"
          />
          <div>
            <p class="eyebrow">SlipTest by SaniSafe Solutions</p>
            <h2 class="h-section mt-1">Pendulum slip testing, written reports, treatment if needed.</h2>
          </div>
        </div>
        <p class="lead mt-5">
          SlipTest is our pendulum slip-resistance testing service. It gives you a defensible, documented
          slip-resistance reading you can hand to an insurer, an auditor or a court.
        </p>
        <ul class="mt-6 grid gap-3 text-base text-ink-charcoal">
          <li class="flex gap-3"><span aria-hidden="true" class="mt-1 inline-block h-2.5 w-2.5 rounded-full bg-brand-yellow"></span>Pendulum (PTV) testing to HSA guidance.</li>
          <li class="flex gap-3"><span aria-hidden="true" class="mt-1 inline-block h-2.5 w-2.5 rounded-full bg-brand-yellow"></span>Written report suitable for insurers, auditors and solicitors.</li>
          <li class="flex gap-3"><span aria-hidden="true" class="mt-1 inline-block h-2.5 w-2.5 rounded-full bg-brand-yellow"></span>Treatment available if PTV is below the recommended threshold.</li>
        </ul>
        <a href="/services/sliptest" class="btn-yellow mt-7">Learn about SlipTest</a>
      </div>

      <div class="lg:col-span-5">
        <div class="rounded-2xl bg-black text-white p-8 shadow-card relative overflow-hidden">
          <svg aria-hidden="true" viewBox="0 0 200 200" class="absolute -right-6 -top-6 h-44 w-44 text-white/15">
            <g fill="none" stroke="currentColor" stroke-width="6">
              <line x1="100" y1="20" x2="100" y2="60" />
              <circle cx="100" cy="80" r="20" />
              <line x1="100" y1="100" x2="40" y2="180" />
              <line x1="40" y1="180" x2="160" y2="180" />
            </g>
          </svg>
          <p class="text-xs font-semibold uppercase tracking-[0.18em] text-brand-yellow">Confidence in every step</p>
          <p class="mt-3 font-display font-extrabold text-2xl">Pendulum Slip Testing & Full Insurance Report Service.</p>
          <p class="mt-3 text-white/80">One contractor closes the loop — we test, we treat, we re-test.</p>
        </div>
      </div>
    </div>
  </Section>
</Layout>
